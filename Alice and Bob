class Solution {
    public long flowerGame(int n, int m) {
        long ans = 0;
        ans = ans + (long)((n + 1)/2)*(m/2);
        ans = ans + (long)((m + 1)/2)*(n/2);
        return ans;
    }
}
